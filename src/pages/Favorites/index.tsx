import React from 'react';
import { View } from 'react-native';

import styles from './styles';

function Favorites() { // eslint-disable-line
  return <View style={styles.container} />;
}

export default Favorites;
